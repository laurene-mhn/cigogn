{
  "background_color": "White",
  "description": "Thanks to Cigogn parents can each swipe first name cards in line with their preferences & get match when they both agree",
  "display": "fullscreen",
  "name": "Cigogn",
  "icons": [
    <% files = Dir.entries(Rails.root.join("app/assets/images/icons/")).select {|f| !File.directory? f} %>
    <% files.each_with_index do |file, index| %>
      {
        "src": "<%= image_path "icons/#{file}" %>",
        "sizes": "<%= file.match(/.+-(?<size>\d{2,3}x\d{2,3}).png/)[:size] %>",
        "type": "image/png"
      }<%= "," unless (files.size - 1) == index %>
    <% end %>
  ]
}
