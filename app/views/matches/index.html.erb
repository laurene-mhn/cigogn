<div class="container">
  <div>
  <br>
  <h3><i class="fas fa-baby"></i> Nos matchs</h3>
  <hr>
  <% @matches.each do |match| %>
  <div class="fav-cards <%= match.name.gender %>-fav-cards">
    <div class="fav-content">
      <%= match.name.name.capitalize %>
    </div>
  </div>
  <% end %>
  </div>
  <div class="footer-names"><%= link_to 'Plus de swipe', names_path, class: 'btn btn-form' %>
  </div>
</div>
